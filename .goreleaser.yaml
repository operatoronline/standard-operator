# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj
version: 2

before:
  hooks:
    - go mod tidy
    - go generate ./...
    - go install github.com/tc-hib/go-winres@latest
    - go-winres make --in cmd/operator-launcher/winres/winres.json --out cmd/operator-launcher/rsrc --product-version={{ .Version }} --file-version={{ .Version }}

builds:
  - id: operator
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
      - -X github.com/standardws/operator/cmd/operator/internal.version={{ .Version }}
      - -X github.com/standardws/operator/cmd/operator/internal.gitCommit={{ .ShortCommit }}
      - -X github.com/standardws/operator/cmd/operator/internal.buildTime={{ .Date }}
      - -X github.com/standardws/operator/cmd/operator/internal.goVersion={{ .Env.GOVERSION }}
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/operator
    ignore:
      - goos: windows
        goarch: arm

  - id: operator-launcher
    binary: operator-launcher
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/operator-launcher
    ignore:
      - goos: windows
        goarch: arm

  - id: operator-launcher-tui
    binary: operator-launcher-tui
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/operator-launcher-tui
    ignore:
      - goos: windows
        goarch: arm

dockers_v2:
  - id: operator
    dockerfile: docker/Dockerfile.goreleaser
    extra_files:
      - docker/entrypoint.sh
    ids:
      - operator
    images:
      - "ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER }}/operator"
      - "docker.io/{{ .Env.DOCKERHUB_IMAGE_NAME }}"
    tags:
      - "{{ .Tag }}"
      - "latest"
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/riscv64

archives:
  - formats: [tar.gz]
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: [zip]

nfpms:
  - id: operator
    builds:
      - operator
      - operator-launcher
      - operator-launcher-tui
    package_name: operator
    file_name_template: >-
      {{ .PackageName }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "arm64" }}aarch64
      {{- else if eq .Arch "arm" }}armv{{ .Arm }}
      {{- else }}{{ .Arch }}{{ end }}
    vendor: operator
    homepage: https://github.com/{{ .Env.GITHUB_REPOSITORY_OWNER }}/operator
    maintainer: operator contributors
    description: operator - a tool for managing and running tasks
    license: MIT
    formats:
      - rpm
      - deb
    bindir: /usr/bin

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

# upx:
#    - enabled: true
#      compress: best
#      lzma: true

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
